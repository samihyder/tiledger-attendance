# Vercel / cloud deployment dependencies
# Heavy native packages (face_recognition, opencv, dlib) are NOT included —
# they exceed Vercel's size limits and require physical hardware anyway.
# For local deployment with biometric hardware use requirements-local.txt

flask>=3.0.3
psycopg2-binary>=2.9.9
numpy>=1.24.0
Pillow>=10.0.0
